{
  "name": "blue-horizon-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "WEB APP",
    "MOBILE"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=\"WEB APP\"",
    "build": "npm run build --workspace=\"WEB APP\"",
    "sync": "npm run build --workspace=\"WEB APP\" && npx cap sync android --workspace=\"MOBILE\""
  }
}
